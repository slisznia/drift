import sys.console.out

fn banner() returns String {
    val base: String = "var bindings opt into future mutation"
    return base
}

fn main() returns Void {
    var counter: Int64 = 0
    out.writeln(banner())
    out.writeln(counter)
}
