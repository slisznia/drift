import sys.console.out

fn is_voter(age: Int64) returns Bool {
	return (age >= 18 and age < 130) or age == 0
}

fn main() returns Void {
	val statement: String = "boolean expressions short-circuit"
	out.writeln(statement)
	out.writeln(is_voter(21))
}
