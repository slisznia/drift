import std.console.out;

struct Counter {
  count: Int
}

fn main() -> Int {
  var c: Counter = Counter(count = 0)
  c.count = c.count + 1
  c.count = c.count + 1
  out.writeln("bumped")
  val rc: Int = 0
  return rc
}
