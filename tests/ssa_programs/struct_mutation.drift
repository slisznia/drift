import std.console.out

struct Counter {
  count: Int64
}

fn main() returns Int64 {
  var c: Counter = Counter(count = 0)
  c.count = c.count + 1
  c.count = c.count + 1
  out.writeln("bumped")
  val rc: Int64 = 0
  return rc
}
