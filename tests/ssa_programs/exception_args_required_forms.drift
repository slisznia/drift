exception ReqErr(a: String, b: String)

fn make_error() returns Void {
  throw ReqErr(a = "aa", b = "bb")
}

fn main() returns Int {
  try {
    make_error()
  } catch ReqErr(e) {
    val a_val: String = e.attrs["a"].as_string().unwrap_or("<missing>")
    val b_val: String = e.attrs["b"].as_string().unwrap_or("<missing>")
    return 0
  }
  return 1
}
