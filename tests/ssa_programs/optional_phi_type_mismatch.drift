fn bad(flag: Bool) -> Int {
    var opt: Optional[Int] = drift_optional_int_some(1)
    if flag {
        opt = drift_optional_int_some(2)
    } else {
        opt = 3  // wrong type on the else branch
    }
    return opt.unwrap_or(0)
}
