exception MyError(a: String)

fn make_error() -> Void {
  throw MyError(a = "AA")
}

fn main() -> Int {
  try {
    make_error()
  } catch e {
    val c = e.code
    // consume c to satisfy type checker; nothing to return from catch
    val _ = c
  }
  return 0
}
