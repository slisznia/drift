exception MyError(a: String)

fn make_error() returns Void {
  throw MyError(a = "AA")
}

fn main() returns Int {
  try {
    make_error()
  } catch e {
    val c = e.code
    // consume c to satisfy type checker; nothing to return from catch
    val _ = c
  }
  return 0
}
