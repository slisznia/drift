import std.console.out

fn may_fail_error(flag: Int) returns Int {
  if flag == 0 {
    return 0
  } else {
    return 1
  }
}

fn main() returns Int {
  try {
    may_fail_error(1)
  } catch err {
    out.writeln("err")
    return 1
  }
  out.writeln("ok")
  return 42
}
