import std.console.out;

fn may_fail_error(flag: Int) -> Int {
  if flag == 0 {
    return 0
  } else {
    throw drift_error_new_dummy(flag, "payload", "")
  }
}

fn main() -> Int {
  // Success path: call does not produce an error.
  val y: Int = try may_fail_error(0) catch { 0; }
  out.writeln("ok")
  return 42
}
