import std.console.out

fn may_fail_error(flag: Int) returns Int {
  if flag == 0 {
    return 0
  } else {
    return 1
  }
}

fn main() returns Int {
  // Success path: call does not produce an error.
  val y: Int = try may_fail_error(0) else 0
  out.writeln("ok")
  return 42
}
