import std.console.out;

exception A()
exception B()

fn fire() -> Void {
  throw B()
}

fn main() -> Int {
  try {
    fire()
  } catch A(e) {
    out.writeln("A")
  } catch err {
    out.writeln("fallback")
  }
  return 0
}
