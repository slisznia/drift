import std.console.out

struct Point {
  x: Int
  y: Int
}

fn bump_x(p: &mut Point) returns Void {
  p.x = p.x + 1
  return
}

fn main() returns Int {
  var p: Point = Point(x = 0, y = 0)
  bump_x(&mut p)
  out.writeln("x bumped")
  return 0
}
