import std.console.out

struct Point {
  x: Int
  y: Int
}

fn bump_x(p: &mut Point) returns Void {
  p.x = p.x + 1
}

fn main() returns Int {
  var p: Point = Point(x = 0, y = 0)
  p.x = 41
  p.y = 9
  bump_x(&mut p)
  return p.x
}
