import std.console.out

fn might_fail(x: Int) -> Int {
  if x != 0 {
    throw drift_error_new_dummy(x, "payload", "")
  }
  return x
}

fn main() -> Int {
  try {
    out.writeln("before")
    might_fail(1)
  } catch err {
    out.writeln("caught")
  }
  return 0
}
