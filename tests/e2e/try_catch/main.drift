import std.console.out

fn might_fail(x: Int) returns Int {
  return x
}

fn main() returns Int {
  try {
    out.writeln("before")
    might_fail(1)
  } catch err {
    out.writeln("caught")
  }
  return 0
}
