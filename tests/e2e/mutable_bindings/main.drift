import std.console.out

fn banner() -> String {
  val base: String = "var bindings opt into future mutation"
  return base
}

fn main() -> Int {
  var counter: Int = 0
  counter = counter + 1
  out.writeln(banner())
  return counter
}
