module m

import std.concurrent as conc;

pub fn main() nothrow -> Int {
var t = conc.spawn(| | => { return 7; });
	var res = t.join();
	match res {
		Ok(v) => { return v; },
		Err(_) => { return 1; },
		default => { return 2; }
	}
}
