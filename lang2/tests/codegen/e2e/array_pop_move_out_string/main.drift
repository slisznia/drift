module main

fn main() nothrow -> Int {
	var xs = ["a", "b"];
	val popped = xs.pop();
	val out = match popped {
		Some(v) => { (v == "b") ? 0 : 1 },
		None => { 2 },
	};
	if out != 0 {
		return out;
	}
	if xs.len != 1 {
		return 3;
	}
	val first = xs[0];
	return (first == "a") ? 0 : 4;
}
