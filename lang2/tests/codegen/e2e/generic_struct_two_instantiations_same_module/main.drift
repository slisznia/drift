module main

import m_box;

fn main() nothrow -> Int {
val a = m_box.Box<type Int>(41);
val b = m_box.Box<type String>("x");
	val x = a.get();
	val y = string_eq(b.get(), "x") ? 1 : 0;
	return x + y;
}
