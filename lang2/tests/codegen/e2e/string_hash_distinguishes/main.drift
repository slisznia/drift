module main

import std.core.hash as hash;

fn main() nothrow -> Int {
	val h_empty = hash.hash_value(&"");
	val h_a = hash.hash_value(&"a");
	val h_b = hash.hash_value(&"b");
	if h_empty == h_a {
		return 10;
	}
	if h_a == h_b {
		return 11;
	}
	return 0;
}
