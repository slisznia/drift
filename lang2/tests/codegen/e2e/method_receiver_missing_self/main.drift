module main

struct Point(x: Int);

implement Point {
	// Intentionally invalid: method is missing the receiver parameter.
	fn bump() -> Void {
		// body irrelevant
	}
}

fn main() nothrow -> Int{
	return 0;
}
