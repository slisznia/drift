module main

import std.containers;

fn main() nothrow -> Int {
var dq: std.containers.Deque<Int> = std.containers.deque_new<type Int>();
	dq.push_back(1);
	dq.push_back(2);
	dq.push_back(3);
	dq.push_back(4);
	dq.push_back(5);
	val _a = dq.pop_front();
	val _b = dq.pop_front();
	dq.push_back(6);
	dq.push_back(7);
	var i = 0;
	var acc = 0;
	while i < dq.len() {
		acc = acc * 10 + *dq.at(i);
		i = i + 1;
	}
	return acc;
}
