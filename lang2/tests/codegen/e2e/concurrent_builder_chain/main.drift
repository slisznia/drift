import std.concurrent as conc;

fn main() nothrow -> Int{
	var b = conc.executor_policy_builder();
	b.min_threads(1)
		.max_threads(2)
		.queue_limit(3)
		.timeout(conc.Duration(millis = 4))
		.on_saturation(conc.SaturationPolicy::ReturnBusy());
	var _p = b.build();
	return 0;
}
