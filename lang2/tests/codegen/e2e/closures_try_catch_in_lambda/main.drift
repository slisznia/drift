module main

exception Boom()

fn may_fail(flag: Int) -> Int {
	if flag == 0 {
		return 41;
	}
	throw Boom();
}

fn main() nothrow -> Int{
	return (|| => {
		val x: Int = try may_fail(0) catch { 0 };
		return x + 1;
	})();
}
