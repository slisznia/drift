module main

struct Inner(arr: Array<Int>);
struct Wrap(inner: Inner);

fn main() nothrow -> Int{
	var w = Wrap(Inner([0, 0]));
	val i = 0;
	val j = 1;
	val p = &w.inner.arr[i];
	w.inner.arr[j] = 1;
	return 0;
}
