module m

import std.concurrent as conc;

pub fn main() nothrow -> Int {
	var r = conc.sleep(conc.Duration(millis = 0));
	match r {
		Err(conc.ConcurrencyError::Timeout()) => { return 1; },
		Ok(_) => { return 2; },
		default => { return 3; }
	}
}
