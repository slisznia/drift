module main

struct Point(x: Int);
struct Other(x: Int);

implement Point {
	fn bad(self: &mut Other) -> Void {
		// Intentionally invalid: receiver type does not match the implement target.
		self->x += 1;
	}
}

fn main() nothrow -> Int{
	return 0;
}
