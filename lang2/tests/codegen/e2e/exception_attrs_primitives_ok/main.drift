module main

exception E(code: Int, fatal: Bool, msg: String)

fn fail() -> Int {
	throw E(code = 123, fatal = true, msg = "hello");
}

fn main() nothrow -> Int{
    try fail() catch err {
        print("caught");
    }
    return 0;
}
