module main

variant Maybe<T> {
	Some(value: T),
	None,
}

fn main() nothrow -> Int{
	val x: Maybe<Int> = Maybe::Some<type Int>(2);
	val y = match x{
	Some(v) => { v },
		None => { 0 },
	};
	return y;
}
