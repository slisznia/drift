module main

import m_box
import m_trait as t

use trait t.Show

fn main() nothrow returns Int{
	val b: m_box.Box = try m_box.make_box(4) catch { m_box.Box(value = 0) };
	return try b.show() catch { 0 };
}
