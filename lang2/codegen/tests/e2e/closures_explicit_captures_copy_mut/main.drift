module main

fn bump(p: &mut Int, by: Int) nothrow returns Int {
	*p += by;
	return *p;
}

fn main() nothrow returns Int{
	var x = 5;
	val i = 2;
	return (| | captures(copy i, &mut x) => { return bump(x, i); })();
}
