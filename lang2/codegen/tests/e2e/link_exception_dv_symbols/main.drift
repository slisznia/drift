module main

exception E(n: Int, s: String, b: Bool)

fn fail() -> Int {
	throw E(n = 1, s = "x", b = false);
}

fn main() nothrow -> Int{
    try fail() catch err {
        print("ok");
    }
    return 0;
}
