module main

exception E(n: Int, s: String, b: Bool)

fn fail() returns Int {
	throw E(n = 1, s = "x", b = false);
}

fn main() returns Int  nothrow{
    try fail() catch err {
        print("ok");
    }
    return 0;
}
