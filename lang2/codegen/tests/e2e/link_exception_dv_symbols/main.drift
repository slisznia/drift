module main

exception E(n: Int, s: String, b: Bool)

fn fail() returns Int {
	throw E(n = 1, s = "x", b = false);
}

fn main() nothrow returns Int{
    try fail() catch err {
        print("ok");
    }
    return 0;
}
