fn id(s: String) returns String { return s; }

fn main() returns Int {
    if id("x") == id("x") {
        if id("x") == id("y") {
            return 1;
        } else {
            return 0;
        }
    }
    return 1;
}
