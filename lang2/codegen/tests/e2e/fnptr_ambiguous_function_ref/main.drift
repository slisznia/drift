module main

fn abs(x: Int) nothrow returns Int {
	if x < 0 {
		return 0 - x;
	}
	return x;
}

fn abs(x: Float) nothrow returns Float {
	return x;
}

fn main() nothrow returns Int{
	val fp = abs;
	return 0;
}
