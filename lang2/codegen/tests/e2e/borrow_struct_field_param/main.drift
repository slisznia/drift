struct Point(x: Int, y: Int)

fn inc_x(p: &mut Point) returns Void nothrow {
	val rx = &mut (*p).x;
	*rx = *rx + 1;
}

fn main() returns Int  nothrow{
	var p = Point(41, 0);
	inc_x(&mut p);
	return p.x;
}
