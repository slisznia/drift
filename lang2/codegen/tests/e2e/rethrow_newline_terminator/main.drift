module m

exception E()

fn main() returns Int {
    try {
        try {
            throw E();
        } catch err {
            print("inner");
            rethrow
        }
        print("UNREACHABLE");
    } catch err {
        print("outer");
    }
    return 0;
}
