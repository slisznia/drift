module m

struct Point(x: Int)
struct Other(x: Int)

implement Point {
	fn bad(self: &mut Other) returns Void {
		// Intentionally invalid: receiver type does not match the implement target.
		self->x += 1;
	}
}

fn main() nothrow returns Int{
	return 0;
}
