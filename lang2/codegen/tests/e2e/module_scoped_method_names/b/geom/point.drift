module b.geom

export { Point, make }

pub struct Point { x: Int, y: Int }

implement Point {
	pub fn move_by(self: &mut Point, dx: Int, dy: Int) returns Void {
		self->x += dx
		self->y += dy
	}
}

pub fn make() returns Point {
	return Point(x = 2, y = 0)
}
