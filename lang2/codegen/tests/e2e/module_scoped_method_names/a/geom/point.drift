module a.geom

export { Point, make }

pub struct Point { x: Int, y: Int }

implement Point {
	pub fn move_by(self: &mut Point, dx: Int, dy: Int) -> Void {
		self->x += dx;
		self->y += dy;
	}
}

pub fn make() -> Point {
	return Point(x = 1, y = 0);
}
