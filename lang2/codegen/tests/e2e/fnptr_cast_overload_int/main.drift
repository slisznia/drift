module main

fn abs(x: Int) nothrow returns Int {
	if x < 0 {
		return 0 - x;
	}
	return x;
}

fn abs(x: Float) nothrow returns Float {
	return x;
}

fn main() nothrow returns Int{
	val fp = cast<fn(Int) nothrow returns Int>(abs);
	val y = fp(-3);
	return y;
}
