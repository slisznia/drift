struct Inner(arr: Array<Int>)
struct Wrap(inner: Inner)

fn main() returns Int  nothrow{
	var w = Wrap(Inner([0, 0]));
	val i: Int = 0;
	val j: Int = 1;
	val p = &w.inner.arr[i];
	w.inner.arr[j] = 1;
	return 0;
}
