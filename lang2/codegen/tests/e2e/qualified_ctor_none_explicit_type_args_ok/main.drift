module main

variant Maybe<T> {
	Some(value: T),
	None,
}

fn main() nothrow -> Int{
	val x = Maybe<Int>::None();
	val y = Maybe<Int>::Some(1);

	val a = match x{
	Some(v) => { v },
		None => { 0 },
	};
	val b = match y{
	Some(v) => { v },
		None => { 0 },
	};
	return a + b;
}
