module main

exception Boom()

fn boom() returns Int {
	throw Boom();
}

fn safe() nothrow returns Int {
	val x: Int = try boom() catch { 0 };
	return x;
}

fn main() nothrow returns Int {
	return safe();
}
