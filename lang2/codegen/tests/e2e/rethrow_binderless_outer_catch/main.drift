module main

exception E()

fn run() returns Int {
    try {
        try {
            throw E();
        } catch {
            print("inner");
            rethrow;
        }
        print("UNREACHABLE");
    } catch {
        print("outer");
    }
    return 0;
}

fn main() returns Int  nothrow{
    try run() catch { return 0; }
    return 0;
}
