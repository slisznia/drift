module main

exception Boom()

fn foo() -> Int {
	throw Boom();
}

fn bar() -> Int {
	throw Boom();
}

fn main() nothrow -> Int{
	try {
		foo();
		bar();
	} catch {
		print("caught");
	}
	return 0;
}
