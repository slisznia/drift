module main

exception Boom()

fn foo() returns Int {
	throw Boom();
}

fn bar() returns Int {
	throw Boom();
}

fn main() returns Int  nothrow{
	try {
		foo();
		bar();
	} catch {
		print("caught");
	}
	return 0;
}
