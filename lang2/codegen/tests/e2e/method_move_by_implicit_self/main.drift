module main

struct Point(x: Int, y: Int);

implement Point {
	fn move_by(self: &mut Point, dx: Int, dy: Int) nothrow -> Void {
		self->x += dx;
		self->y += dy;
	}
}

fn main() nothrow -> Int{
	var p: Point = Point(1, 2);
	p.move_by(10, 20);
	return p.x + p.y;
}
