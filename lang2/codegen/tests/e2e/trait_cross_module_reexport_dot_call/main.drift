module main

import m_api
import m_box

use trait m_api.Show

fn main() returns Int  nothrow{
	val b: m_box.Box = try m_box.make_box(5) catch { m_box.Box(value = 0) };
	return try b.show() catch { 0 };
}
