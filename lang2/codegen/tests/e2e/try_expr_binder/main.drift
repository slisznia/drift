module m

exception Boom()

fn may_fail(flag: Int) returns Int {
	if flag == 0 {
		return 0;
	}
	throw Boom {};
}

fn main() returns Int {
	val x: Int = try may_fail(1) catch err {
		println("caught");
		42 };
	return x;
}
