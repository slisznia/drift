module m

exception Boom()

fn may_fail(flag: Int) -> Int {
	if flag == 0 {
		return 0;
	}
	throw Boom();
}

fn main() nothrow -> Int{
	val x: Int = try may_fail(1) catch err {		println("caught");
		42 };
	return x;
}
