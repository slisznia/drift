module main

exception E()

fn run() returns Int {
    try {
        try {
            throw E();
        } catch {
            rethrow;
        }
    } catch {
        print("handled");
    }
    return 0;
}

fn main() returns Int  nothrow{
    try run() catch { return 0; }
    return 0;
}
