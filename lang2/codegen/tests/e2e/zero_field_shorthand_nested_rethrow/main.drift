module main

exception E()

fn run() -> Int {
    try {
        try {
            throw E();
        } catch {
            rethrow;
        }
    } catch {
        print("handled");
    }
    return 0;
}

fn main() nothrow -> Int{
    try run() catch { return 0; }
    return 0;
}
