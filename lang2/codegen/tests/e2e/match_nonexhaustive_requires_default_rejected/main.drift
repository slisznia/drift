module main

variant Maybe<T> {
	Some(value: T),
	None,
}

fn main() nothrow -> Int{
	val x: Maybe<Int> = None();
	// Non-exhaustive match without `default` is rejected in MVP.
	val y = match x{
	Some(v) => { v },
	};
	return y;
}
