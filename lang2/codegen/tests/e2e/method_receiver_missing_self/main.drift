module m

struct Point(x: Int)

implement Point {
	// Intentionally invalid: method is missing the receiver parameter.
	fn bump() returns Void {
		// body irrelevant
	}
}

fn main() returns Int  nothrow{
	return 0
}
