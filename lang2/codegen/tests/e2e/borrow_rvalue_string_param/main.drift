fn byte_len_ref(s: &String) returns Int nothrow {
	return byte_length(s);
}

fn main() returns Int  nothrow{
	// Shared borrow of an rvalue string literal is materialized into a hidden temp.
	return byte_len_ref(&("abc"));
}
