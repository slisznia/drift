module m

exception A()
exception B()

fn main() returns Int {
    try {
        try {
            throw A();
        } catch m:A(a) {
            print("inner");
            throw B();
        }
        print("UNREACHABLE");
    } catch m:B(b) {
        print("outer");
    }
    return 0;
}
